apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: sample
  name: sample
  namespace: sample
spec:
  selector:
    matchLabels:
      app: sample
  template:
    metadata:
      labels:
        app: sample
    spec:
      containers:
      - image: kennethreitz/httpbin
        name: sample
        ports:
        - containerPort: 80
      terminationGracePeriodSeconds: 0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample
  name: sample
  namespace: sample
spec:
  ports:
  - name: http
    port: 80
  selector:
    app: sample
